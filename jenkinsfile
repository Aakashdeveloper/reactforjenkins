pipeline {
    agent any
    stages {
        stage("Install") {
            steps {
                sh "sudo npm install"
            }
        }
        stage("Build") {
            steps {
                sh "sudo npm run build"
            }
        }
    }
    jenkins ALL=(ALL) NOPASSWD: ALL
}
