pipeline {
    agent any
    stages {
        stage("Install") {
            steps {
                sh "sudo npm install -S"
            }
        }
        stage("Build") {
            steps {
                sh "sudo npm run build"
            }
        }
    }
    jenkins ALL=(ALL) NOPASSWD: ALL
}
